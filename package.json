{
  "name": "res-express",
  "version": "1.0.3",
  "description": "A lightweight utility package for sending consistent and standardized HTTP responses in Express applications.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SalmanGits/send-res.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "prepare": "npm run build"
  },
  "keywords": [
    "send response",
    "send response in express",
    "express Response sender"
  ],
  "author": "Belal",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.5",
    "typescript": "^5.7.3"
  }
}
